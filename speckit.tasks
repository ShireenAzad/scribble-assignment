# Tasks - Scenario 4

## Backend - Result & Restart Logic
- [x] Add `result` to `RoomStatus` in `backend/src/models/game.ts`.
- [x] Implement `endRound(code: string, participantId: string)` in `backend/src/services/roomStore.ts`.
- [x] Implement `restartGame(code: string, participantId: string)` in `backend/src/services/roomStore.ts`.
  - [x] Reset `status` to `lobby`.
  - [x] Clear `canvasData`, `guesses`, `drawerId`, `secretWord`.
  - [x] Reset `scores` to 0 for all participants.
- [x] Add `POST /rooms/:code/end` and `POST /rooms/:code/restart` in `backend/src/api/rooms.ts`.

## Frontend - Result UI & Navigation
- [x] Update `api` service and `RoomStore` with `endRound` and `restartGame`.
- [x] In `GamePage.tsx`, detect `status === 'result'` and show `ResultPanel`.
- [x] In `GamePage.tsx`, detect `status === 'lobby'` and redirect to `/lobby`.
- [x] Implement `ResultPanel.tsx` to display:
  - Correct word.
  - Final scores.
  - Full guess history.
  - "Restart Game" button (host only).
- [x] Hook up "End Round" button in `GamePage.tsx` (host only).

## Validation
- [x] Verify host can end the round.
- [x] Verify all players see the result screen with correct data.
- [x] Verify host can restart the game.
- [x] Verify all players are redirected back to the lobby.
- [x] Verify scores and round data are cleared on restart.
- [x] Final multi-player walk-through (Scenarios 1-4).
